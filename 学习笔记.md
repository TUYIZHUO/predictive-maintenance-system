#一、 AI 编程工具学习总结
  Vibe Coding 是一种“人机协作”的开发范式——开发者通过高质量提示词与AI模型交互，让 AI 生成代码、测试用例、架构设计，开发者则负责需求拆解、代码审查、调试与集成。其核心是用 AI 加速开发，用人工保证质量。
  在AI编程主要有以下技巧：
   任务拆解。将大需求拆成“数据预处理→模型训练→API 开发→前端展示”等小任务，每次只让 AI 处理一个子任务，降低幻觉风险。
   约束说明。在 Prompt 中明确输入输出格式、异常处理方式、性能要求（例如：“用 PyTorch 实现 LSTM，输入维度为 8，输出二分类概率”）。
   验收标准。提前设定标准，如测试集准确率 ≥ 85%，API 响应时间 < 200ms，并让 AI 自测后再提交。
   上下文工程。让 AI 先读取现有代码文件，再提出修改建议；使用项目规则文件，保持风格一致。
   规格驱动开发。先写简单的需求文档，让 AI 制定实现计划，分步编码，每步都进行人工验收。
   自动化测试。让 AI 生成 pytest 单元测试，覆盖核心函数，每次修改后自动运行，及早发现问题。
   人工接管。当 AI 连续犯错时，需要回到上一个稳定版本，缩小改动范围，或提供最小复现示例让 AI 针对性修复。
  遇到AI生成了不兼容的库版本，需要在 Prompt 中明确指定版本号。模型训练代码未处理数据泄露时，要求 AI 显式写出 train_test_split 且 shuffle=True。前端与后端接口字段不一致时，使用 OpenAPI/Swagger 生成契约，AI 按契约生
  成代码。生成的代码过于冗长时，要求“重构为更简洁的写法”或“抽取公共函数”。

#二、拟选用的 Harness 与模型
##1.编程大模型选用
  DeepSeek-V4：用于核心编码、架构设计、代码重构。在中文工业场景理解上表现优异，且支持长上下文。
  Qwen 3.8：用于数据预处理脚本、自动化测试生成。对 Python 数据处理库（pandas/numpy）掌握熟练。
  Claude Code：用于前端界面（Streamlit/Vue）代码。擅长生成美观的 UI 组件，且能同步生成说明文档。
##2.Harness工具选用
  搭配使用以下 IDE 插件/工具，提升协作效率：
  Trae：嵌入式 AI 对话，快速解释代码段、生成注释。
  通义灵码：代码补全、单元测试生成、Bug 修复建议。
  GitHub Copilot：日常代码补全，加速重复性编码工作。
##3.本项目的核心算法模型
  LSTM 分类器：用于故障二分类（是否故障）及多分类（5 种故障类型）。
  LSTM 自编码器：用于异常检测，基于重构误差识别早期退化。
  遗传算法（DEAP 库）：用于生成最优维护调度计划，优化目标为最小化停机时间+最大化设备利用率。

#三、Git原理与功能
##1.Git核心
  工作区（Working Directory）：本地的项目文件夹。
  暂存区（Staging Area）：git add后的临时存放区域。
  本地仓库（Local Repository）：.git目录，记录所有提交历史。
  远程仓库（Remote Repository）：托管在 GitHub 上的备份，用于协作与备份。
##2.常用命令与用途
  git init：初始化本地仓库。
  git clone <url>：克隆远程仓库到本地。
  git status：查看当前文件状态。
  git add <file>：将修改加入暂存区。
  git commit -m "message"：提交暂存区快照到本地仓库。
  git push origin main：将本地提交推送到远程仓库。
  git pull origin main：拉取远程更新并合并。
  git branch / git checkout：分支管理与切换。
  git log --oneline：查看简洁提交历史。
3.Git使用规范
  每完成一个功能模块（例如“完成数据清洗脚本”、“训练 LSTM 模型并评估”）就执行一次 commit。
  使用“动词 + 名词”格式，如 feat: add LSTM autoencoder for anomaly detection。
  main 分支存放稳定版本，dev 分支用于日常开发，feature/* 用于新功能开发。

#四、选题调研
##1.为什么选择预测性维护场景
  实现价值：制造企业设备非计划停机造成的损失巨大，预测性维护能显著降低停机风险。
  技术适配：预测性维护场景覆盖至少 3 个技术方向：深度学习、模式识别、智能优化，本选题结合了这三种技术方向。
  数据可得：选题所用的AI4I 2020 数据集是公开的 UCI 标准数据集，可直接下载使用，无企业敏感数据。
##2.行业背景与研究意义
  在工业 4.0 背景下，设备维护经历了“事后维修 → 预防性维护 → 预测性维护”的演进。预测性维护（PdM）通过传感器数据与机器学习模型，预测设备剩余寿命（RUL）并提前预警，是智能制造的核心应用之一。
##3.现有解决方案的不足
  传统方法中阈值报警误报率高，且无法区分故障类型。
  部分深度学习模型，如纯 CNN忽视时序依赖，预测精度有限。
  多数研究仅关注是否故障缺乏对何时维护以及如何调度的闭环决策。
##4.数据集选择
  AI4I 2020 数据集由 UCI Machine Learning Repository 公开发布。该数据集是UCI专门为铣床预测性维护算法基准测试而设计发布的，特征维度覆盖空气温度、工艺温度、转速、扭矩、刀具磨损等 5 类传感器与工艺参数，与选题场景高度匹配。数据同时提供了二分类标签（是否故障）和 5 种具体故障类型标签（TWF/HDF/PWF/OSF/RNF），既可做故障检测也可做故障模式识别，能够支撑深度学习、模式识别和智能优化三个技术方向。数据集中正常样本约 96%、故障样本约 4% 的不平衡分布真实反映了工业场景中故障稀疏的特点。此外，该数据集模拟的是工业铣床的真实加工过程，其生成逻辑基于 2860W 恒定功率、40Nm 平均扭矩等物理参数，故障触发条件有明确的物理规则（如刀具磨损超过 200 分钟触发 TWF），保证了数据背后存在真实的业务逻辑而非纯数字模拟。
##5.选题所使用的技术
  深度学习：采用 LSTM（长短期记忆网络）处理传感器时序数据，捕捉温度、转速、扭矩等特征在时间维度上的依赖关系，实现故障二分类（是否故障）与多分类（5 种具体故障类型）预测。
  模式识别：采用 LSTM 自编码器进行无监督异常检测，仅用正常样本训练，通过计算输入与重构输出之间的重构误差来识别设备早期退化信号，当误差超过阈值时触发预警。
  智能优化：采用遗传算法对维护计划进行优化调度，综合考虑设备故障风险等级、维护资源约束和停机成本，生成最优维护优先级排序，实现从“预测”到“决策”的闭环。
  数据处理：采用 StandardScaler 对数值特征进行标准化，消除量纲影响；采用 SMOTE（合成少数类过采样技术）处理数据集中故障样本仅占 4% 的类别不平衡问题。
  系统架构：采用 FastAPI 构建高性能异步后端服务，Streamlit和ECharts搭建前端仪表盘实现数据可视化，SQLite和SQLAlchemy实现数据持久化存储。
  开发工具：使用Git进行版本管理，小步提交并保留过程档案；借助 DeepSeek-V4、Qwen 3.8 等大模型辅助代码生成与调试。
##6.本项目的创新点
  融合 LSTM 与自编码器：既做分类又做异常检测，双模互补提高预警可靠性。
  引入优化算法：将预测结果与遗传算法结合，给出具体维护计划，形成“感知-预测-决策”闭环。
  完整 B/S 架构：前端看板、后端 API、数据库、算法模块分离，具备工业级系统雏形。
